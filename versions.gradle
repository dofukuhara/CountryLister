ext {
    compileSdkVersion = 32
    targetSdkVersion = 32
    minSdkVersion = 21

    // General libraries version
    gson_version = "2.10"

    // Main module specific libraries version
    fragment_ktx_version = "1.5.5"

    // Network Module specific libraries version
    retrofit_version = "2.9.0"
    okhttp3_logging_version = "5.0.0-alpha.10"
}